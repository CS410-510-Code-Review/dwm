name: Pull Requests

on: pull_request
jobs:
  ci-build:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 10
    - run: git fetch --no-tags --prune --depth=10 origin main
    - name: build
      run: make
